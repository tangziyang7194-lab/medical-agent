# AI 导诊系统 - 本地运行（无需 Docker）

## 🎯 最简单的运行方式

### Windows 用户（推荐）

1. **双击运行 `一键启动.bat`**

2. **首次运行会自动**:
   - 安装 Python 依赖包
   - 创建配置文件 `.env`
   - 打开记事本让你编辑配置

3. **编辑配置**:
   - 在记事本中找到 `ZHIPUAI_API_KEY=`
   - 填入你的 API Key（从 https://open.bigmodel.cn 获取）
   - 保存并关闭

4. **按回车继续**，系统会自动启动

5. **浏览器访问**:
   - 用户端: http://localhost:5000
   - 管理端: http://localhost:5000/admin/login

---

### Linux/macOS 用户

1. **运行启动脚本**:
   ```bash
   python 启动.py
   ```

2. **首次运行会自动**:
   - 检查 Python 版本
   - 安装依赖包
   - 创建配置文件 `.env`

3. **编辑配置**:
   ```bash
   nano .env
   ```
   - 填入 `ZHIPUAI_API_KEY=your_key_here`
   - 保存退出

4. **重新运行**:
   ```bash
   python 启动.py
   ```

5. **浏览器访问**:
   - 用户端: http://localhost:5000
   - 管理端: http://localhost:5000/admin/login

---

## 📋 前置条件

### 1. Python

**检查是否已安装**:
```bash
python --version
```

**如果没有安装**:
- Windows: https://www.python.org/downloads/
- 下载 Python 3.8 或更高版本
- **重要**: 安装时勾选 "Add Python to PATH"

### 2. 获取 API Key

1. 访问 https://open.bigmodel.cn/
2. 注册/登录
3. 进入控制台 → API Keys
4. 创建新的 API Key
5. 复制并保存

---

## 🔧 常见问题

### Q: 提示 "未找到 Python"

**A:**
- Windows: 从 https://www.python.org/downloads/ 下载安装
- 安装时务必勾选 "Add Python to PATH"
- 重新打开命令行窗口

### Q: 提示 "端口被占用"

**A:**
- 检查是否有其他程序使用了 5000 端口
- 或修改 `.env` 文件中的 `FLASK_PORT=5001`

### Q: 提示 "依赖包安装失败"

**A:**
- 手动安装: `pip install -r requirements.txt`
- 或使用国内镜像: `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### Q: 打不开网页

**A:**
- 检查服务器是否正常运行（命令行会显示 "Running on http://127.0.0.1:5000"）
- 确认端口没被防火墙阻止

---

## 💡 快速测试

启动后访问 http://localhost:5000，应该看到：

- ✅ 首页显示 "AI 导诊系统"
- ✅ 可以点击 "用户端" 或 "管理端"
- ✅ 用户可以注册和登录
- ✅ 管理员可以注册和登录

---

## 🛑 停止服务器

- **命令行中按 Ctrl+C**

---

## 📝 配置文件说明

`.env` 文件中的关键配置：

```bash
# 必填
ZHIPUAI_API_KEY=your_api_key_here

# 数据库配置（可选，使用 SQLite 不需要配置）
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=medical

# Flask 配置（可选）
FLASK_HOST=127.0.0.1
FLASK_PORT=5000
FLASK_DEBUG=True
```

---

## 🎉 完成！

现在你可以在浏览器中使用 AI 导诊系统了！

- 用户可以进行问诊、生成诊断报告
- 管理员可以管理系统和样例库

---

**有问题？查看 `CLOUD_DEPLOYMENT.md` 或 `DEPLOYMENT_CHECKLIST.md`**